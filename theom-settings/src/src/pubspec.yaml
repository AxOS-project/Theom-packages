name: theom_settings
description: "Settings of theom desktop environment."
publish_to: "none"

version: 1.11.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  process_run: ^0.12.3
  file_picker: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

[package]
name = "theom-dashboard"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
slint = "1.8.0"
whoami = "1.6.0"
winit = "=0.28.7" 
chrono = "0.4"

[build-dependencies]
slint-build = "1.8.0"
